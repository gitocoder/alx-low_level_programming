#include <stdlib.b>
#include <time.h>
/*more header goes there*/
/*betty stylel doc for function main goes there*/
int main(void)
{
	int n;
	stand(time(0));
	n = rand() - RAND_MAX / 2;
	/*your code goes there*/
	m = n % 10 
	if (m > 5)
		printf("last digit of %i is %i and is grater than 5/n")

	return (0);
}
